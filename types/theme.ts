export type Theme = {
  name: string;
  bgStops: [string, string];
  cardStroke: string;
  divider: string;
  panelBg: string;
  panelStroke: string;

  textStrong: string;
  textMuted: string;
  textDim: string;

  accentStops: [string, string, string];
  chartFill: string;

  listLabel: string;
  listValue: string;
  listIcon: string;
};
